// codegen.h — DECLARES generateRestockCode()
// If this declaration does NOT match the .c file, linker errors happen.

#ifndef CODEGEN_H
#define CODEGEN_H

// Returns a pointer to a static char array containing a restock code
char* generateRestockCode();

#endif
